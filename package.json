{
  "name": "dienstplanrad",
  "private": true,
  "version": "0.1.0",
  "description": "Manueller Dienstplaner für Radiologie auf Cloudflare Pages",
  "type": "module",
  "scripts": {
    "check": "node --check js/app.js && node --check js/api.js && node --check js/defaults.js && node --check js/holidays.js && node --check js/legacy-worker.js && node --check js/rules.js && node --check js/state.js && node --check js/theme.js && node --check functions/_utils.js",
    "test": "node --test tests/*.test.js",
    "deploy-note": "Deploy via Cloudflare Pages from GitHub repository root"
  }
}
